Top-up Frontend

Quickstart:

```bash
cd topup-frontend
npm install
npm run dev
```

The frontend assumes the backend is proxied or served under the same origin, so requests to `/api/*` will reach the backend. In development you can configure Vite proxy to your backend server if needed.
